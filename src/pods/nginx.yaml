apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
    somelabel: hz
  name: nginx
spec:
  containers:
  - image: nginx:1.27.0
    name: nginx
    ports:
    - containerPort: 80
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Never
status: {}
